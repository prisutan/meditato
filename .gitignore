PROJECT_NOTES.md
CONTENT_LOG.md
MONETIZATION_PLAN.md
.claude/
content/
design/
Meditato Logo files/
_site/
.jekyll-cache/
.bundle/
vendor/

# Pre-resize source images — never the final asset, always superseded by the
# resized file in the same folder. Delete after resizing; this is a backstop
# in case that's forgotten.
assets/img/**/*-original.*
